// window is a global object jo browser ke ander js ka engine hota hai wo hamare liya available kar deta hai

// window ke ander jo function hai wo basically humara browser ke functionality ko acess karne ko help karta hai

// 'window' is not part of our javascript engine. It is part of browser. Browser define 'window' object

// Like when we type console.log ,it acess from window object

// We can type window.console.log()

// window.document - This will acess the html/ It will point html . It is not necessary write window. We can type only 'document'

// console.log(window);

/**Here if we run the above code then it will give error but it we run this code in browser then it will run smoothly
 * Because 'window' object is not part of javascript engine , it is part of browser only
 * Thats why the upper code will run smoothly in browser not in node/javascript engine
*/

document.write("Hello from js")

/**
 * Using the upper code js add the text in our existing html file. This is call DOM manupulation
 */

/**
 * In js have two property -> 1) kab karna hai 2) Karna kaya hai
 */